<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chỉnh sửa thông tin xe</title>
    <!-- Thêm Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body>
    <div class="container mt-5">
        <h2>Chỉnh sửa xe: <%= car.tenXe %>
        </h2>
        <a href="/cars" class="btn btn-secondary">Quay lại</a> <br><br>
        <form action="/cars/update/<%= car._id %>" method="POST">
            <!-- Tên xe -->
            <div class="mb-3">
                <label for="tenXe" class="form-label">Tên xe</label>
                <input type="text" class="form-control" name="tenXe" value="<%= car.tenXe %>" id="tenXe"
                    placeholder="Tên xe">
            </div>

            <!-- Hãng sản xuất -->
            <div class="mb-3">
                <label for="hangSanXuat" class="form-label">Hãng sản xuất</label>
                <input type="text" class="form-control" name="hangSanXuat" value="<%= car.hangSanXuat %>"
                    id="hangSanXuat" placeholder="Hãng sản xuất">
            </div>

            <!-- Năm sản xuất -->
            <div class="mb-3">
                <label for="namSanXuat" class="form-label">Năm sản xuất</label>
                <input type="number" class="form-control" name="namSanXuat" value="<%= car.namSanXuat %>"
                    id="namSanXuat" placeholder="Năm sản xuất">
            </div>

            <!-- Giá bán -->
            <div class="mb-3">
                <label for="giaBan" class="form-label">Giá bán</label>
                <input type="number" class="form-control" name="giaBan" value="<%= car.giaBan %>" id="giaBan"
                    placeholder="Giá bán">
            </div>

            <!-- Mô tả -->
            <div class="mb-3">
                <label for="moTa" class="form-label">Mô tả</label>
                <textarea class="form-control" name="moTa" id="moTa" placeholder="Mô tả"><%= car.moTa %></textarea>
            </div>

            <!-- Nút gửi -->
            <button type="submit" class="btn btn-primary">Cập nhật xe</button>
        </form>
    </div>

    <!-- Thêm Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>